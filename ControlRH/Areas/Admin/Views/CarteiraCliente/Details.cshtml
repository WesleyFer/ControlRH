@model CarteiraClienteViewModel

@{
    ViewData["Title"] = "Detalhes Carteira de Cliente";
}

<div class="card">
    <div class="card-header">
        Detalhes da Carteira de Cliente
    </div>
    <div class="card-body">
        <p><strong>Nome:</strong> @Model.Nome</p>
        <p><strong>Criado em:</strong> @DateTime.Now.ToString("dd/MM/yyyy HH:mm")</p>

        <hr />
    </div>
    <div class="card-footer text-center">
        <a asp-area="Admin" asp-controller="CarteiraCliente" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-sm btn-primary btn-custom">Editar</a>
        <a asp-area="Admin" asp-controller="CarteiraCliente" asp-action="Index" class="btn btn-sm btn-secondary btn-custom">Voltar</a>
    </div>
</div>